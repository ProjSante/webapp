sudo: false

language: node_js

node_js:
  - node

install:
  - npm ci
  - npm i jest
  - cd client && npm ci && cd ../

# script:
#   - npm run test

script:
  - npm run test:site
  - set NODE_ENV=test
  - npm run test:server

notifications:
  email: false